# GIF finder in flutter
This project searches for GIF images using the Giphy API. It searches for GIFs after N milliseconds and shows the results in a grid.
## Requirements

 - Flutter (Project uses v3.0.5)
